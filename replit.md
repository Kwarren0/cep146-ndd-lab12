# CEP 146 - NDD Lab 12

## Project Overview
A C-language networking lab project for CEP 146 (Networking course).

## Structure
- `main.c` - Main C source file (starter placeholder)
- `Makefile` - Build configuration using gcc
- `lab12` - Compiled binary (gitignored)

## Build & Run
```bash
make        # Compile
./lab12     # Run
make clean  # Remove compiled binary
```

## Workflow
- **Start application**: Runs `make && ./lab12` (console output)

## Notes
- Uses gcc with `-Wall -Wextra -g` flags
- This is a fresh lab scaffold; replace `main.c` with actual lab code as needed
